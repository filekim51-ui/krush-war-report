services:
  - type: web
    name: coc-war-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: COC_TOKEN
        sync: false  # you'll paste it in the dashboard